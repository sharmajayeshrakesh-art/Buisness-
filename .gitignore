# Local dev tooling (Playwright verification harness) — not part of the deliverable
# Anchored to repo root so the spline-app/ project's own package.json is NOT ignored.
/node_modules/
/package.json
/package-lock.json

# Scratch / test artifacts
*.png
test3d.test.html
index.test.html
